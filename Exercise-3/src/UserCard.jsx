
    // Destructing the props
    export const UserCard = ({name, email}) => {
        return(
            <div>
                <h4>Name: {name}</h4>
                <h4>Email: {email}</h4>
            </div>
        )
    }